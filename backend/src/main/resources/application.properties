spring.application.name=backend

# MongoDB
spring.data.mongodb.uri=mongodb+srv://smartcampus:%23MarNif2003@cluster0.zxnz9x3.mongodb.net/smartcampus?retryWrites=true&w=majority&appName=Cluster0
spring.data.mongodb.database=smartcampus

# Server
server.port=8080

# Google OAuth2
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=email,profile

# JWT
app.jwt.secret=unisphere-super-secret-jwt-key-2026-group94-sliit
app.jwt.expiration=86400000

# Frontend URL
app.frontend.url=http://localhost:5173

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
app.mail.from=${MAIL_USERNAME}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
